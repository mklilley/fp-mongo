# Mongo

Some example code for helping to teach people how to work with MongoDB.

## Set-up and run
- Clone this repo
- `cd fp-mongo`
- `docker compose up -d`
- `docker exec -it mongo bash`
- `mongosh`
